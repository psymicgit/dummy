#include "baseobj.h"
#include <logic/player/player.h>


/*
VOID CBaseObj::SetSaveState(ESaveState eSaveState ) 
{ 
	if(m_poOwner)
	{
		m_poOwner->SetUpdateTime();
		if(ESS_WANT_SAVE == eSaveState)
		{
			m_poOwner->SetSaveState(ESS_WANT_SAVE);
		}		
	}

	SetSaveState2(eSaveState);
}
*/

