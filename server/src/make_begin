compile_type = release
# ${shell echo $DEBUG}
long_bit 	 = ${shell getconf LONG_BIT}

# µ±Ç°Ä¿Â¼
cur_dir := ${shell pwd}

bin_dir 	 = ${base_dir}/bin/linux${long_bit}
lib_dir 	 = ${base_dir}/lib/linux${long_bit}/${compile_type}
3rd_dir		 = ${base_dir}/3rd
src_dir 	 = ${base_dir}/src